name: multi_platform_course
description: Laboratory work for the course of multiplatform developing

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1

environment:
  sdk: ">=2.10.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.0
  sqflite: ^1.3.2+1
  path: ^1.7.0
  bloc: ^6.1.0
  equatable: ^1.2.5
  get_it: ^5.0.1
  path_provider: ^1.6.24
  mockito: ^4.1.3


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true